# Semantic Memory

This note explains that the service indexes markdown content, supports search, and returns readable snippets.
A query like semantic memory or search snippets should find this document.
